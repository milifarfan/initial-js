const names = ['Ana', 'Lola', 'Lenny'];

const num_characters = names.map( name => `${name} has ${name.length} letters`);

console.log (num_characters);

//(parametro) => {
  //  return
//}

//const num_characters = names.map(function(name){
  //  return `${name} has ${name.length} letters`;
//});
